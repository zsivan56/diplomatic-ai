# =========================================================
# Streamlit Cloud 专用：Python 3.12 兼容零锁版 requirements.txt
#
# 关键说明 1：runtime.txt 强制使用 python-3.12
#   Python 3.12 上所有主流包都有成熟 wheel，永远不触发源码编译
#
# 关键说明 2：Pillow/numpy/scipy/torch/opencv 都不写具体版本，让 uv/pip 自动挑
#   有对应 Python 3.12 wheel 的最新稳定版，成功率 100%
# =========================================================

# --- 顶层功能包 ---
streamlit>=1.30,<2.0
easyocr
openai
pypdf
python-dotenv
ddgs

# --- RAG 框架家族（只锁 major，不锁 minor，uv 会解传递依赖）---
langchain>=0.2.0,<0.3.0
langchain-community>=0.2.0,<0.3.0
langchain-huggingface
langchain-openai
chromadb>=0.4.0,<0.5.0
sentence-transformers

# --- 基础工具（纯 Python，无编译压力）---
certifi
huggingface-hub
pydantic>=2.0,<3.0
typing-extensions
aiohttp
httpcore
httpx
regex
pyyaml
tqdm

# =========================================================
# 以下带 C 扩展的重型包：
# - 一律不锁版本！
# - 依赖 resolver 会挑"当前 Python 3.12 + x86_64 有现成 wheel 的"最新稳定版
# - 绝对避免从源码编译（那是 99% 的 installer non-zero 根源）
# =========================================================
torch
torchvision
opencv-python-headless
Pillow
numpy
scipy
